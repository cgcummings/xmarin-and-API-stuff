<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PhoneApp.Views.ItemDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15">
        <Label Text="Title:" FontSize="Medium" />
        <Editor Text="{Binding Text}" FontSize="Small"/>
        <Label Text="Instruction:" FontSize="Medium" />
        <Editor Text="{Binding Description}" FontSize="Small"/>
        <Label Text="Ingredients:" FontSize="Medium" />
        <Editor Text="{Binding Ingredients}" FontSize="Small"/>
        <Label Text="Instructions:" FontSize="Medium" />
        <Editor Text="{Binding Instruction}" FontSize="Small"/>
        <StackLayout Orientation="Horizontal">
            <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
            <Button Text="Save" Command="{Binding SaveCommand}" HorizontalOptions="FillAndExpand"></Button>
        </StackLayout>
    </StackLayout>

</ContentPage>